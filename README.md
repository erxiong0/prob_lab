# prob_lab
To understand the function of each layer's representation of LLM

## 环境（uv）

```bash
# 创建虚拟环境并安装依赖
uv venv
source .venv/bin/activate   # macOS/Linux
# .venv\Scripts\activate    # Windows

uv pip install -r requirements.txt
# 或：uv sync   # 使用 pyproject.toml 中的依赖
```
